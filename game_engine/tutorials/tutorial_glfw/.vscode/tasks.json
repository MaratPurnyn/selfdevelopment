{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "glfw",
            "type": "shell",
            "command": "g++ src/main.cpp src/window.cpp src/log.cpp -I/usr/local/include -L/usr/local/lib -lglfw -lGL -lGLU -lGLEW -o bin/tutorial_glfw.out",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}